const hello = 'Tap on the console and insert a command.'
const prompt = 'Command:'
const result = 'IP address:'
const error = 'Received error with code:'

module.exports = {
  hello: hello,
  prompt: prompt,
  result: result,
  error: error
}
